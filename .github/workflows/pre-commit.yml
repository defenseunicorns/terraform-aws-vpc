# If the workflow trigger is "pull_request", run pre-commit checks.
name: pre-commit

on:
  pull_request:

permissions:
  pull-requests: write
  id-token: write
  contents: read

jobs:

  get-workflow-access-token:
    uses: defenseunicorns/delivery-github-actions-workflows/.github/workflows/get-workflow-token.yml@main
    secrets:
      APPLICATION_ID: ${{ secrets.NARWHAL_BOT_APP_ID }}
      APPLICATION_PRIVATE_KEY: ${{ secrets.NARWHAL_BOT_SECRET }}

  debug:
    needs: get-workflow-access-token
    runs-on: ubuntu-latest
    steps:
    - uses: hmarr/debug-action@v2

  pre-commit:
    uses: defenseunicorns/delivery-github-actions-workflows/.github/workflows/pre-commit.yml@main
    secrets:
      token: ${{ needs.get-workflow-access-token.outputs.token }}
    with:
      check-types: '["common", "terraform", "golang"]'
